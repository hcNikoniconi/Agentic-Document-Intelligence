# Application paths. Relative paths are resolved by the application process.
APP_BASE_DIR=.
INPUT_ROOT=./data
TEMPLATE_DIR=./templates
OUTPUT_ROOT=./output

# OpenAI-compatible model service (for example, a remote vLLM server).
MODEL_BASE_URL=http://127.0.0.1:8000/v1
MODEL_API_KEY=replace-me
MODEL_NAME=qwen3-8b

# Gradio service. Keep 127.0.0.1 when accessing through an SSH tunnel.
APP_HOST=127.0.0.1
APP_PORT=7860
